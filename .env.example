# Omada API Configuration
# Create an application at Settings > Platform Integration > Open API in your Omada Controller
# Use "Client Credentials Mode" when creating the application

OMADA_CLIENT_ID=your_client_id_here
OMADA_CLIENT_SECRET=your_client_secret_here
OMADA_ID=your_omada_id_here

# Optional: Leave empty to show site selection dropdown
# Set this to skip site selection and use a default site
OMADA_SITE_ID=

# API Base URL - Choose one based on your controller type:

# CLOUD CONTROLLER (CBC):
# - US Region: https://use1-omada-northbound.tplinkcloud.com
# - Europe Region: https://euw1-omada-northbound.tplinkcloud.com
# - Singapore Region: https://aps1-omada-northbound.tplinkcloud.com
OMADA_API_BASE_URL=https://use1-omada-northbound.tplinkcloud.com

# LOCAL CONTROLLER (Self-hosted):
# Find your Interface Access Address at Settings > Platform Integration > Open API
# Examples:
# - Local network: https://192.168.1.100:8043
# - Localhost: https://localhost:8043
# - Custom domain: https://omada.yourdomain.com:8043
# OMADA_API_BASE_URL=https://192.168.1.100:8043

# SSL Certificate Validation (for local controllers with self-signed certificates)
# IMPORTANT: Only disable for local development or trusted internal networks
# Set to '0' to accept self-signed certificates (USE WITH CAUTION)
# Leave unset or set to '1' for production environments with valid certificates
# NODE_TLS_REJECT_UNAUTHORIZED=0

# OPTIONAL: Cleanup Schedule
# Cron expression for automatic cleanup of expired users
# Default: '0 * * * *' (every hour at minute 0)
# Examples:
# - '0 * * * *'  = Every hour
# - '*/30 * * * *' = Every 30 minutes
# - '0 0 * * *'  = Daily at midnight
# - '0 */6 * * *' = Every 6 hours
# CLEANUP_SCHEDULE='0 * * * *'
